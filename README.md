# 🌦️ Weather App

A simple and responsive Weather Application built using **JavaScript** and the **OpenWeatherMap API**. This app allows users to search for any city and get real-time weather updates.

## ✨ Features
- **Search by Location**: Get current weather data for any city worldwide.
- **Live Data**: Displays temperature in Celsius and weather conditions (e.g., Clouds, Rain).
- **Dynamic Icons**: Shows weather icons directly from the OpenWeatherMap service.
- **Responsive Design**: Built with a clean interface using CSS Flexbox.

## 🛠️ Technologies Used
- **HTML5**: For the application structure.
- **CSS3**: For styling and layout.
- **JavaScript (ES6)**: For fetching data from the API and DOM manipulation.
- **OpenWeatherMap API**: The data source for weather information.

## 🚀 How to Use
1. Clone or download the repository.
2. Open `app.js` and insert your API key into the `apiKey` variable.
3. Open `index.html` in your browser.

## 📌 Note
This project was developed for a presentation. Ensure you have an active internet connection to fetch live data.
